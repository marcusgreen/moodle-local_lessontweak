define("local_lessontweak/dragreorder",["exports","core/config"],(function(_exports,_config){var obj;
/**
   * Drag-to-reorder pages in the collapsed lesson editor.
   *
   * Drives the lesson module's existing move action
   * (lesson.php?action=moveit&pageid=..&after=..) so no core change is needed.
   * On drop the row order is read from the DOM and the browser navigates to the
   * move URL; the server reorders and reloads the editor.
   *
   * @module     local_lessontweak/dragreorder
   * @copyright  2026 Marcus Green
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_config=(obj=_config)&&obj.__esModule?obj:{default:obj};const pageIdOf=row=>{if(!row)return 0;const anchor=row.querySelector('a[id^="lesson-"]');return anchor&&parseInt(anchor.id.replace("lesson-",""),10)||0};_exports.init=(cmid,sesskey)=>{const table=(()=>{const anchor=document.querySelector('table a[id^="lesson-"]');return anchor?anchor.closest("table"):null})();if(!table)return;const tbody=table.querySelector("tbody")||table;let dragging=null;tbody.querySelectorAll("tr").forEach((row=>{if(!pageIdOf(row))return;row.setAttribute("draggable","true"),row.classList.add("lessontweak-draggable"),row.querySelectorAll("a, img").forEach((el=>el.setAttribute("draggable","false")));const firstCell=row.querySelector("td, th");if(firstCell&&!firstCell.querySelector(".lessontweak-handle")){const handle=document.createElement("span");handle.className="lessontweak-handle",handle.setAttribute("aria-hidden","true"),handle.textContent="⠇",firstCell.insertBefore(handle,firstCell.firstChild)}row.addEventListener("dragstart",(e=>{dragging=row,row.classList.add("lessontweak-dragging"),e.dataTransfer.effectAllowed="move",e.dataTransfer.setData("text/plain",String(pageIdOf(row)))})),row.addEventListener("dragend",(()=>{if(!dragging)return;row.classList.remove("lessontweak-dragging");const after=pageIdOf(dragging.previousElementSibling),pageid=pageIdOf(dragging);dragging=null;const url=_config.default.wwwroot+"/mod/lesson/lesson.php?id="+encodeURIComponent(cmid)+"&sesskey="+encodeURIComponent(sesskey)+"&action=moveit&pageid="+encodeURIComponent(pageid)+"&after="+encodeURIComponent(after);window.location.assign(url)}))})),tbody.addEventListener("dragover",(e=>{if(!dragging)return;e.preventDefault(),e.dataTransfer.dropEffect="move";const before=((container,y,dragging)=>{const rows=[...container.querySelectorAll("tr")].filter((r=>r!==dragging));for(const row of rows){const box=row.getBoundingClientRect();if(y<box.top+box.height/2)return row}return null})(tbody,e.clientY,dragging);before?tbody.insertBefore(dragging,before):tbody.appendChild(dragging)})),tbody.addEventListener("drop",(e=>{dragging&&e.preventDefault()}))}}));

//# sourceMappingURL=dragreorder.min.js.map